# SPA fallback for react-router (BrowserRouter): serve index.html for any
# path that is not a real static file, so deep links / refresh work on
# Cloudflare Pages. Real files (/assets/*, /locales/*, /favicon.svg) are
# served directly and are not affected by this rewrite.
/*    /index.html   200
